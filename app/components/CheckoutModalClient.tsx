"use client";

import { CheckoutModal, type CheckoutModalProps } from "./CheckoutModal";

export default function CheckoutModalClient(props: CheckoutModalProps) {
  return <CheckoutModal {...props} />;
}
